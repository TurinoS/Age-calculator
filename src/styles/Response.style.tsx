import styled from "styled-components";

const StyledSection = styled.section`
    padding: 3em;
    
    & h2 {
        font-size: 64px;
    }

    & span {
        color: var(--bg-darker);
    }
`

export default StyledSection;